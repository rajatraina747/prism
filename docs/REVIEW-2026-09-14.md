# Third-Party Review — 14 September 2026 (v1.8.0 → v1.8.1)

Point-in-time review of v1.8.0 (`a051dd4`) by an outside team acting as
cybersecurity, product, design and engineering. Re-verified the September
hardening ([AUDIT-2026-09.md](AUDIT-2026-09.md)), covered the surface it did
not (Transfers page, player chrome, extension, librqbit persistence), and
turned the findings into the arcs now in [ROADMAP.md](../ROADMAP.md).
Every item below was checked against the code, and the two P0 bugs against
the maintainer's live app data.

## P0 — bugs found in use (fixed in v1.8.1)

**Finished downloads never reached the Library.** The archive effect in
`src/stores/AppProvider.tsx` was a 2 s timer keyed on the whole queue array;
every progress tick rebuilt the array and re-armed the timer, so with any
transfer active nothing was ever archived. The Transfers page hides finished
rows, so completed items vanished. Fixed by keying the effect on the set of
terminal ids; regression test added.

**Flat multi-file torrents shared one folder.** `torrent.rs` passed an
explicit `output_folder`, which disables librqbit's default of a
`<dest>/<name>` subfolder for multi-file torrents. Two F1 packs with the same
inner file names were writing interleaved pieces into one
`02.Race.Session.mp4`. Fixed by deriving the folder from the metainfo before
the add (list-only fetch for magnets without cached metadata), sanitising the
name to one path component, refusing a folder already owned by a different
info hash, and carrying the effective folder into the completion event and
Library. Six Rust tests added.

## Security

The September fixes hold: no raw mpv permissions in any capability, mpv runs
`config=no load-scripts=no ytdl=no`, `--` terminator before URLs, https-only
blocklist, capped fetches, SHA-pinned actions, empty `audit.toml` ignore
list, signed updater, Sentry doubly gated and scrubbed, extension has one
permission and no native messaging, no localhost listener anywhere. No
critical or remotely exploitable-without-user-action flaw.

| # | Sev | Finding | Status |
|---|-----|---------|--------|
| S-1 | High (needs a webview compromise first) | librqbit `session.json` lives in `$APPDATA`, writable by the webview (`fs:allow-appdata-write-recursive`); persisted `output_folder` is re-added at startup with `overwrite: true`, bypassing the path validators. | **Fixed 1.9.0**: the main window's fs capability allows only top-level `$APPDATA/*.json` reads and `*.json.tmp` writes/renames (no `fs:default`); enforced end to end by a mock-runtime IPC/ACL test. |
| S-2 | High on Windows / Medium elsewhere | Managed yt-dlp in `$APPDATA/engine` is preferred over the sidecar with no hash re-check; Windows needs no exec bit. | **Fixed 1.9.0**: out of the webview's reach (S-1), and its SHA-256 is recorded at install and re-checked before use. |
| S-3 | Medium | No `--ignore-config`/`--no-plugin-dirs` on yt-dlp calls; any config file could override the argv (`--exec`). | **Fixed 1.8.1** (`engine.rs` `LOCKDOWN_ARGS`). |
| S-4 | Medium | `validate_open_path` canonicalised for containment but returned the original string; the media allowlist then checked a symlink's own name. | **Fixed 1.8.1** (returns the resolved path; test). |
| S-5 | Medium | macOS libmpv/FFmpeg/x264/x265 still come from unpinned `brew install mpv` at build time. | **Fixed on v2.0**: the macOS bundle ships the LGPL toolchain Prism builds itself from pinned sources, fetched by SHA-256 from a dated release — no Homebrew, and no x264/x265/rubberband to pin. CI unpacks it on a macOS runner every push and reads the licence off the binaries. |
| S-6 | Medium | `magnet:` and `prism://add` deep links act with no confirmation: a web page can make Prism join a swarm or run yt-dlp with the user's cookies. | **Fixed 1.9.0**: OS-originated links wait on a confirmation card; nothing is fetched until Add. |
| S-7 | Medium (Windows) | Vendored mpv plugin falls back to a bare DLL name (CWD search). | **Fixed 1.9.0**: no fallback; a missing or unloadable bundled libmpv is an error. |
| S-8 | Low | No Rust-side cap on yt-dlp spawns; unbounded capture buffers. | **Fixed 1.9.0**: 6 lookups / 16 downloads; 64 MB stdout cap, 256 KB stderr tail. |
| S-9 | Low | Linux deny-list has no non-hidden entries; case-sensitive root compare. | **Fixed 1.9.0**: `~/bin`, `~/Desktop/*.desktop`; case-insensitive on macOS/Windows. |
| S-10 | Low | `build.yml` `contents: write` on every job; minisign key unencrypted; no key backup/rotation doc. | **Fixed 1.9.0** in the workflow and `docs/RELEASE-KEYS.md`; encrypting the key itself is a manual step (see that doc). |
| S-11 | Low | `explorer /select,` built by concatenation with a torrent-controlled name. | **Fixed on v2.0**: `explorer_select_arg` quotes the path and refuses one containing a quote, passed through `raw_arg` so nothing re-splits it; unit-tested both ways. |
| S-12 | Low | Raw yt-dlp stderr (cookie DB paths) returned verbatim as the error string. | **Fixed on v2.0**: failures cross as a structured `PrismError` (`errors.rs`) and the UI classifies by code instead of matching text. `redact()` — home path → `~`, cookie/login-data stores → `[browser profile]`, URL query strings → `?…` — runs inside the constructor, so no caller can forget it; covered by `cookie_stores_and_query_strings_never_leave_rust`. |
| S-13 | Info | Release logs and `mpv.log` contain media paths; Privacy Policy silent on local logs. | Already covered: Privacy Policy → "What stays on your device" names both logs and says they can contain URLs and paths. |
| S-14 | Info | `player` capability grants unused `dialog:default`. | Not an issue: the player window's "Open file" picker (`Player.tsx`) uses it. |
| S-15 | Info | Updater has no downgrade protection. | Accepted. |

Accepted carry-overs from September: proxy creds in argv/settings.json, PATH
augmentation, hardened-runtime relaxations + ad-hoc signing, UPnP/DHT/LSD on
by default (all toggleable).

## Product & engineering health

- ffmpeg is required for merged 4K, audio extraction, embedding and
  SponsorBlock but was never stated. **README fixed 1.8.1**; bundling a static
  LGPL ffmpeg is the v2.0 answer.
- README claimed macOS Intel with no such build job. **Fixed 1.8.1.**
- README screenshots (July) predate Transfers, the detail panel and the player.
- No `SECURITY.md`, issue templates, CONTRIBUTING; About has no "Report a bug".
- Bundle id `com.prism.app` implies ownership of prism.app. Decide the
  migration to `com.rainacorp.prism` at the start of v2.0.
- TypeScript strictness is off; eslint disables `no-unused-vars` /
  `no-explicit-any`. 16 Playwright tests never run in CI.
- 2 `log::` call sites in all of Rust; "Log level" is cosmetic for the backend.
- 7.4 MB of PNG logos (one unreferenced) ship in every installer; 32 of 41
  shadcn files and ~20 Radix deps unused; two reqwest versions in the lock.
- `lib.rs` 1,869 lines / 23 commands; `torrent.rs` thinly tested; the five
  largest page components have zero tests.
- `--force-ipv4` hardcoded; error classification pattern-matches English.
- Minisign key has no backup/rotation doc; updater endpoint is the repo.
- Positioning: "download manager" framing vs no plain-HTTP downloads.

## UX

Good bones: pure guarded reducer, events not polling, `SwarmHealth` copy,
confirm dialogs that name consequences, undo toasts, SetupCard, quality
shortfall surfaced, single icon family, persisted filter/sort.

High: light-mode tokens missing for success/warning/sidebar (**fixed 1.8.1**);
Settings controls unlabelled for assistive tech; drop submitted twice
(**fixed 1.8.1**); quiet hours indistinguishable from slot waiting; failure
suggestion shown in the wrong place and parse errors unclassified; no
virtualization plus per-row `backdrop-filter`; rows not focusable and
`aria-selected` on `listitem`; grip and global ↑/↓ both fire.

Medium: 20-row flat "Downloads" settings section; two bandwidth limits in two
units; settings written per keystroke (**fixed 1.8.1**); Library delete with
no undo; "Undo" on cancel restarts from 0%; Library/Subscriptions lack the
Transfers list model; terminology drift (transfer/queue/download/history);
three post-add behaviours; ephemeral preset silently overridden by per-site
memory; 45 s torrent picker with no escape hatch; unskippable splash; empty
states without actions; no global shortcuts or native menu; legal pages
always return to Settings; picker rows and tabs lack semantics; no
reduced-motion support.

Jargon without help: info hash, DHT/PEX, pieces map, uTP, LSD, socks5h, p2p
blocklist, announce URLs, remux, VP9/AV1, ratio.

## Ideas (ranked; ★ = next release)

**UI/UX:** ★ one "Add" surface everywhere; ★ Settings restructure with
progressive disclosure; ★ failure UX with actions; ★ quiet-hours banner;
★ light mode + accessibility pass; ★ virtualization and per-row blur
removal; Transfers density modes; Library as a real library (sort,
multi-select, grid, storage tile); global keyboard + native menu; tray/Dock
progress; player chrome (custom selects, chapters, resume position, PiP,
external subs); torrent picker elapsed time + escape hatch; consistent
post-add toast; preset transparency; terminology lock; brand restraint; empty
states with actions; undo that resumes.

**Features:** ★ bundle ffmpeg; ★ engine freshness check; structured errors;
★ watch folder; ★ categories with per-category destination + move-completed;
★ filename templates; content-level duplicate detection; post-completion
actions; per-item scheduling + weekday quiet hours; generic HTTP/FTP engine
(positioning decision); ★ stream-while-downloading; RSS rules; import from
another client; librqbit upstream PRs; conversion presets + chapter
splitting; clip download; site presets beyond quality; Chrome/Edge extension
+ AMO; statistics page; settings/Library export; web UI later; i18n
scaffolding.

## Verification performed (v1.8.1)

- `cargo clippy --all-targets -- -D warnings`: clean. `cargo test --lib`: 48 passed.
- `npx tsc --noEmit`, `npm run lint` (0 errors), `vitest`: 163 passed.
- Not exercised here: the GUI against real torrents — re-add the two flat F1
  packs after upgrading and confirm they land in separate folders.
